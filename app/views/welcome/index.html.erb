

<% if manager_signed_in? %>
		<!-- Administr area --> 			  
		<%= render partial: 'home/manager_view' %>
		

<% elsif franqueado_signed_in? %>
		<%= render partial: 'franqueado_view' %>


<% elsif vistoriadore_signed_in? %>
		<%= render partial: 'vistoriadore_view' %>
		

<% else %>
		<!-- Public area --> 			  
		<%= render partial: 'public_view' %>
<% end %>





</div>