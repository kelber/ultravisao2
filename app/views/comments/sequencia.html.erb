

<!-- Thumbnail link for lion image -->
<a href="#" data-toggle="modal" data-target=".pop-up-2">
 <%= image_tag comment.image.thumb('400x200').url if comment.image_stored?  %>
</a>

<hr>

 <!--  Modal content for the lion image example -->
  <div class="modal fade pop-up-2" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel-2" aria-hidden="true">
    <div class="modal-dialog modal-lg">
      <div class="modal-content">

        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
          <h4 class="modal-title" id="myLargeModalLabel-2">
  				<%= comment.manager_id if current_manager %>
	<%= comment.franqueado_id if current_franqueado %>
	<%= comment.vistoriadore_id if current_vistoriadore %></h4>
        </div>
        <div class="modal-body">
       <%= image_tag comment.image.thumb('400x200').url if comment.image_stored?  %>
        </div>
      </div><!-- /.modal-content -->
    </div><!-- /.modal-dialog -->
  </div><!-- /.modal mixer image -->

  <h3 align="left">  
 	 <%=  comment.body %></h3>
	
</div>